<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="tech.ula.vm" />
        <package android:name="tech.ula.qemu" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_main_launcher"
        android:roundIcon="@mipmap/ic_main_launcher_round"
        android:supportsRtl="true">

        <receiver
            android:name="tech.ula.library.ui.InstallPairingReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="tech.ula.library.INSTALL_PAIR" />
            </intent-filter>
        </receiver>

        <!-- Cross-app target for UserLAnd VM's/UserLAnd QEMU's own notification "Stop
             Session"/"Settings" actions; see CompanionNotificationActionReceiver's doc
             comment. Unlike InstallPairingReceiver above (a same-app PendingIntent reply target,
             correctly unexported), this genuinely needs exported="true": the companion app
             targets it by explicit component from its own process. No permission gate is used
             here deliberately; see the doc comment for why that's an accepted tradeoff. -->
        <receiver
            android:name="tech.ula.library.ui.CompanionNotificationActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="tech.ula.STOP_SESSION" />
                <action android:name="tech.ula.OPEN_SETTINGS" />
            </intent-filter>
        </receiver>

        <activity android:name="tech.ula.library.MainActivity" android:launchMode="singleTask" android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|density|screenLayout" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
